[package]
name = "regret-verify"
version = "0.1.0"
edition = "2021"
description = "Independent verification fixture for the Rust Regrets stack (PR #355)"

[lib]
name = "regret_verify"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
