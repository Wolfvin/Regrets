[package]
name = "regret-example"
version = "0.1.0"
edition = "2021"
description = "Example Rust library for Regrets regression testing"

[lib]
name = "regret_example"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
